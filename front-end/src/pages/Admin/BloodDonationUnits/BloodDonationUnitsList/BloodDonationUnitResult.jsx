// import React from 'react';

// const BloodDonationUnitResult = ({ unitSearchResults }) => {
//   return (
//     <div className="unit-results">
//       {unitSearchResults.length > 0 ? (
//         unitSearchResults.map((unit) => (
//           <div key={unit.id} className="unit-item" style={{ border: '1px solid #ccc', margin: '10px 0', padding: '10px', borderRadius: '4px' }}>
//             <h3>{unit.name}</h3>
//             <p><strong>Location:</strong> {unit.location}</p>
//             <p><strong>Phone:</strong> {unit.phone}</p>
//           </div>
//         ))
//       ) : (
//         <p>No units found.</p>
//       )}
//     </div>
//   );
// };

// export default BloodDonationUnitResult;
